#include "main.h"
/**
 * rev_string - a function that reverses a string
 * @s: input string pointer
 * Return: Nothing
 */
void rev_string(char *s)
{
	int i;

	while (str[i] != '\0')
		i++;
	while (i)
		_putchar(s[i--]);

}
